# Task List

Scope: Phase 1 only - Data Model, Seed Data, And Core Domain Logic.

Valid Task statuses: OPEN, PREPARED, REJECTED, PASSED
ID: Use growing unique integers

| ID | Component | Static Aspect | Status | Retries | Description | Depends On (ID) | Testing Coverage Exceptions | Verification Criteria |
|----|-----------|---------------|--------|---------|-------------|-----------------|-----------------------------|-----------------------|
| 14 | Phase 1 boundary | Planning and guardrails | OPEN | 0 | Confirm Phase 1 implements only schema, seed data, typed constants, pure domain logic, and audit helper boundaries; do not add player UI, team login, quest routes, admin review UI, map UI, or deployment work. | - | Documentation-only review is excluded from coverage. | A brief implementation note or PR summary states the Phase 1 boundary; code review finds no Phase 2+ behavior. |
| 15 | Domain constants | Core rules | OPEN | 0 | Define typed constants for exactly 2 teams, 25 quests, 21 approvals required for final reveal, submission statuses `pending`/`approved`/`rejected`, team quest statuses `not_started`/`pending_review`/`approved`/`rejected`/`skipped`, supported proof kinds, and rejection reasons. | 14 | None. | Unit tests cover exported constants, enum/status unions, and invalid status/reason handling where runtime validation exists. |
| 16 | Domain model types | Core rules | OPEN | 0 | Define TypeScript types for teams, quests, team quest progress, submissions, audit logs, optional app settings, proof kinds, rejection reasons, and map progress snapshots. | 15 | Type-only files may be excluded through explicit coverage config patterns. | Typecheck passes; runtime helpers that consume the types are covered by tests. |
| 17 | Supabase migration structure | Data model | OPEN | 0 | Add the project migration location and first Phase 1 SQL migration files for the MVP tables and database constraints. | 14 | SQL migration files are static database artifacts and may be excluded from JS/TS coverage. | Migration files exist in the documented location and are ordered/named consistently for Supabase usage. |
| 18 | Teams schema | Data model | OPEN | 0 | Create the `teams` table with stable IDs, display names, PIN hash/storage field, per-team progress counters or derivable fields, timestamps, and constraints for exactly two seeded teams. | 17 | SQL migration files are excluded from JS/TS coverage. | Schema includes primary key, unique team identity/name constraints, required fields, and timestamp defaults. |
| 19 | Quests schema | Data model | OPEN | 0 | Create the `quests` table with unguessable slug, title, flavor text, instructions, success criteria, safety warning, proof type, optional hint text, active flag, and ordering/metadata needed for seed validation. | 17 | SQL migration files are excluded from JS/TS coverage. | Schema enforces unique nonempty slugs and supports all 25 seeded quest records without media upload fields. |
| 20 | Team quest progress schema | Data model | OPEN | 0 | Create `team_quest_progress` with team/quest foreign keys, status, hint timestamp, approval timestamp, skipped timestamp, and uniqueness for one progress row per team and quest. | 18, 19 | SQL migration files are excluded from JS/TS coverage. | Schema has foreign keys, valid status constraint, and a unique `(team_id, quest_id)` constraint. |
| 21 | Submissions schema | Data model | OPEN | 0 | Create `submissions` with team/quest foreign keys, contributor name, proof kind, proof value, optional note, status, rejection reason, rejection message, submitted/reviewed timestamps, and no direct media upload columns. | 18, 19 | SQL migration files are excluded from JS/TS coverage. | Schema supports pending, approved, rejected submissions and stores proof as link/text only. |
| 22 | Submission uniqueness rules | Data model | OPEN | 0 | Add database-level protection against more than one active pending or approved submission for the same team and quest while allowing new submissions after rejection. | 21 | SQL migration files are excluded from JS/TS coverage. | A partial unique index or equivalent constraint blocks duplicate `pending`/`approved` submissions per team/quest and permits rejected history. |
| 23 | Audit and app settings schema | Data model | OPEN | 0 | Create `audit_logs` for major actions and optional `app_settings` for required approval count and game pause state. | 17, 18, 19, 21 | SQL migration files are excluded from JS/TS coverage. | Audit schema records actor type/id, action, team, quest, submission, metadata, and timestamp; settings default to 21 required approvals if included. |
| 24 | Schema indexes and integrity | Data model | OPEN | 0 | Add indexes and constraints needed for common Phase 2/3 queries: quest by slug, pending submissions, team progress lookup, audit by created time, and foreign-key cascade/restrict behavior. | 18, 19, 20, 21, 22, 23 | SQL migration files are excluded from JS/TS coverage. | Migration contains explicit indexes/constraints for all MVP lookup paths and preserves team privacy by data shape. |
| 25 | Seed data structure | Seed data | OPEN | 0 | Add seed files or seed builders for the two teams, 25 quests, and initial team quest progress rows for every team/quest pair. | 18, 19, 20 | Static seed data may be excluded only if it has no executable logic; seed builders and validators require coverage. | Seed data contains exactly 2 teams, exactly 25 quests, and exactly 50 initial progress rows with `not_started` status. |
| 26 | Unguessable quest slugs | Seed data | OPEN | 0 | Generate and store 25 stable unguessable quest slugs suitable for QR URLs, with no predictable numeric or title-derived-only sequence. | 25 | Static seed data may be excluded only if it has no executable logic; slug validators require coverage. | Tests or validation checks confirm all slugs are unique, nonempty, URL-safe, and not simple sequential identifiers. |
| 27 | Seed validation helpers | Seed data | OPEN | 0 | Add pure validation helpers that verify seed counts, unique slugs, proof types, active defaults, team count, and complete team quest progress coverage. | 15, 25, 26 | None. | Unit tests cover valid seed data and failure branches for missing teams, duplicate slugs, wrong quest count, invalid proof type, and missing progress rows. |
| 28 | Map progress logic | Core domain logic | OPEN | 0 | Implement pure functions that calculate approved quest count, revealed fragment count, capped progress, and final unlock state from team quest progress. | 15, 16 | None. | Unit tests cover 0 approvals, partial progress, exactly 21 approvals, more than 21 approvals, skipped/rejected/pending statuses, and final unlock. |
| 29 | Duplicate submission logic | Core domain logic | OPEN | 0 | Implement pure rules that decide whether a team can submit proof for a quest based on existing submission and progress state. | 15, 16, 21, 22 | None. | Unit tests prove pending and approved submissions block duplicates, rejected submissions allow resubmission, and unrelated team/quest submissions do not block. |
| 30 | Submission creation logic | Core domain logic | OPEN | 0 | Implement pure helpers for creating a pending submission draft and moving team quest progress to `pending_review`, including proof kind/value and contributor validation. | 15, 16, 29 | None. | Unit tests cover valid URL proof, valid text proof, missing contributor, missing proof value, wrong proof kind, and resulting pending state. |
| 31 | Rejection and resubmission logic | Core domain logic | OPEN | 0 | Implement pure helpers for rejecting a submission, storing rejection reason/message, marking progress as `rejected`, and allowing immediate resubmission. | 15, 16, 29, 30 | None. | Unit tests cover every rejection reason, optional admin message, rejected progress state, and the resubmission allowance after rejection. |
| 32 | Approval logic and idempotency | Core domain logic | OPEN | 0 | Implement pure helpers for approving a submission, marking team quest progress as `approved`, setting timestamps, and preventing a second approval from incrementing map progress twice. | 15, 16, 28, 29, 30 | None. | Unit tests cover first approval, duplicate approval/idempotent approval, approval after rejection/resubmission, and unchanged progress for already approved quests. |
| 33 | Audit log helper | Core domain logic | OPEN | 0 | Add a typed helper for constructing audit log records for major Phase 1 domain actions: submission created, submission approved, submission rejected, progress changed, seed/schema maintenance where relevant. | 16, 23, 30, 31, 32 | None. | Unit tests cover actor fields, action names, related team/quest/submission IDs, metadata preservation, and missing optional relations. |
| 34 | Supabase type boundary | Data access boundary | OPEN | 0 | Extend the existing Supabase boundary with generated or hand-maintained database types for Phase 1 tables without adding live UI flows or browser-facing data access. | 16, 17, 18, 19, 20, 21, 23 | Type-only generated files may be excluded through explicit coverage config patterns. | Typecheck passes; any non-type boundary helpers are unit tested at 100%. |
| 35 | Phase 1 integration coverage | Test coverage | OPEN | 0 | Add Vitest coverage for combined domain scenarios: duplicate submission prevention, rejection/resubmission, approval-driven map progress, and 21-approval final unlock. | 27, 28, 29, 30, 31, 32, 33 | Test files themselves may be excluded from coverage. | `npm test` passes and scenario tests exercise the required acceptance criteria end to end within pure domain logic. |
| 36 | Coverage gate verification | Quality gate | OPEN | 0 | Keep V8 coverage thresholds at 100% statements, branches, and functions for all committed Phase 1 application code. | 35 | SQL/static seed/type-only/config files may be excluded only through explicit coverage config comments or patterns. | `npm run coverage` passes at 100% statements, branches, and functions. |
| 37 | Build and lint verification | Quality gate | OPEN | 0 | Verify Phase 1 changes compile and lint under the existing strict TypeScript and Next.js configuration. | 34, 36 | None. | `npm run build` and `npm run lint` pass from the repository root. |
| 38 | Phase 1 completion review | Quality gate | OPEN | 0 | Review the implementation against Phase 1 acceptance criteria and confirm schema, seed data, domain logic, audit helper, and quality gates are complete without Phase 2+ scope creep. | 14, 24, 26, 33, 35, 36, 37 | Documentation-only review is excluded from coverage. | Review confirms schema supports MVP flows without direct media storage, seeds create two teams and 25 unguessable quest slugs, required domain tests pass, audit helper exists, and all verification commands pass. |
