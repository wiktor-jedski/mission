# Task List

Scope: Phase 5 only - Admin Tools, Audit Log, Hints, And Polling.

Valid Task statuses: OPEN, PREPARED, REJECTED, PASSED
ID: Use growing unique integers

| ID | Component | Static Aspect | Status | Retries | Description | Depends On (ID) | Testing Coverage Exceptions | Verification Criteria |
|----|-----------|---------------|--------|---------|-------------|-----------------|-----------------------------|-----------------------|
| 150 | Phase 5 boundary | Planning and guardrails | PASSED | 0 | Confirm Phase 5 implements only audit log view, hint usage tracking, admin manual overrides, replacement proof entry, and pending submission polling; do not add visual polish, reveal animations, sound, intro, pause/resume, deployment work, direct media upload, user-controlled CSS, raw HTML, or arbitrary style content. | - | Documentation-only review is excluded from coverage. | Code review confirms all Phase 5 changes stay within the Phase 5 acceptance criteria from `docs/implementation-plan.md`. |
| 151 | Current audit coverage inventory | Audit log | PASSED | 0 | Inventory existing audit writes for team login, quest view, proof submission, hint usage, approval, rejection, and overrides so Phase 5 fills only missing action coverage. | 150 | Documentation-only inventory is excluded from coverage. | A short implementation note or code comments identify every audited action and every newly added audit event. |
| 152 | Audit action constants | Domain logic | PASSED | 0 | Add or normalize typed audit action names for login, quest view, submission, hint usage, approval, rejection, manual reveal, manual hide, quest skip, broken quest override, replacement proof entry, and generic admin override. | 151 | None. | TypeScript exposes stable audit action values and tests fail on unknown or misspelled action names. |
| 153 | Audit metadata validation | Domain logic | PASSED | 0 | Define small pure helpers for building audit metadata for hints and admin overrides without storing secrets, team PINs, admin passwords, raw cookies, or unnecessary proof content. | 152 | None. | Unit tests cover full, minimal, and invalid metadata inputs with 100% coverage. |
| 154 | Team login audit write | Runtime persistence | PASSED | 0 | Ensure successful team login writes an audit event scoped to the team; invalid PIN attempts must not reveal team state and should not store submitted PIN values. | 152, 153 | None. | Tests verify successful login writes a team login audit event and invalid login does not leak secret input. |
| 155 | Quest view audit write | Runtime persistence | PASSED | 0 | Record a quest view audit event when a logged-in team opens a valid quest slug. | 152, 153 | None. | Tests verify valid quest views are audited with team and quest IDs while unknown slugs remain safe. |
| 156 | Submission audit write review | Runtime persistence | PASSED | 0 | Confirm or add proof submission audit events for new submissions and resubmissions after rejection. | 152, 153 | None. | Tests verify first submission and rejected resubmission each write one submission audit event. |
| 157 | Hint reveal domain rule | Domain logic | PASSED | 0 | Add pure domain logic for revealing or using a hint: only valid team/quest progress can be updated, hint usage is idempotent, and no time penalty or map progress change is applied. | 150 | None. | Unit tests cover first use, repeated use, missing hint text, already approved quests, rejected quests, and no map-progress side effect. |
| 158 | Hint persistence | Runtime persistence | PASSED | 0 | Persist hint usage by setting `team_quest_progress.hint_used_at` and writing a hint usage audit event. | 157, 152, 153 | SQL migration files may be excluded through explicit coverage config patterns; reusable logic must be covered. | Integration tests verify hint timestamp persistence and audit creation for local and Supabase repository paths where applicable. |
| 159 | Hint player action endpoint | Player route/API | PASSED | 0 | Add a player-scoped server action or route for revealing a quest hint that requires a valid team session and valid quest slug. | 158 | Next.js route wrappers may be excluded only if they contain no business logic; reusable handlers must be covered. | Tests verify unauthenticated access is rejected, wrong/unknown quest slugs are safe, and another team's state is not modified. |
| 160 | Hint UI behavior | Player UI | PASSED | 0 | Add a functional hint control on quest pages that reveals optional hint text and reflects whether the team has already used the hint. | 159 | Static copy may be excluded only through explicit coverage config patterns. | Component/integration tests verify hidden hint, reveal action, already-used state, no-hint state, and accessible labels. |
| 161 | Audit log query model | Admin data access | PASSED | 0 | Implement admin-only audit log retrieval with newest-first ordering, team/quest/submission context fields, and a bounded default result limit. | 152 | None. | Tests verify ordering, limit behavior, context mapping, empty state, and admin-only access. |
| 162 | Audit log admin route | Admin route/API | PASSED | 0 | Add an admin-protected audit log page or route that loads audit events without exposing team PINs, admin password, or raw session secrets. | 161 | Next.js route wrappers may be excluded only if they contain no business logic; reusable handlers must be covered. | Tests verify unauthenticated users are redirected or rejected and authenticated admin users can load audit events. |
| 163 | Audit log admin UI | Admin UI | PASSED | 0 | Display audit events for login, quest view, submission, hint usage, approval, rejection, and overrides with timestamp, action, team, quest, submission, actor, and concise metadata. | 162 | Static formatting copy may be excluded only through explicit coverage config patterns. | Component tests cover populated, empty, unknown metadata, and long metadata states without exposing secrets. |
| 164 | Admin override domain model | Domain logic | PASSED | 0 | Define pure domain functions for manual fragment reveal, manual fragment hide, quest skip, broken quest override, and replacement proof entry with explicit status/map-progress effects. | 150 | None. | Unit tests cover every override path, invalid state transitions, idempotency, map-progress bounds, and final unlock boundary behavior. |
| 165 | Manual reveal rule | Domain logic | PASSED | 0 | Implement manual map fragment reveal so admin can increase a team's revealed fragment count up to the required approval count without approving a quest. | 164 | None. | Tests verify increment behavior, upper bound at 21 fragments, audit metadata, and independence between teams. |
| 166 | Manual hide rule | Domain logic | PASSED | 0 | Implement manual map fragment hide so admin can decrease a team's revealed fragment count without deleting approved submissions or exposing other team state. | 164 | None. | Tests verify decrement behavior, lower bound at 0 fragments, final photo relock behavior if progress falls below 21, and audit metadata. |
| 167 | Quest skip rule | Domain logic | PASSED | 0 | Implement admin quest skip for a team so a broken or unsuitable quest can be marked skipped without creating an approved submission unless explicitly overridden. | 164 | None. | Tests verify skipped status, skipped timestamp, no duplicate active submission, resubmission policy if applicable, and audit metadata. |
| 168 | Broken quest override rule | Domain logic | PASSED | 0 | Implement admin override for a broken quest that can mark a team quest approved and reveal the next fragment once, with a required reason. | 164 | None. | Tests verify approval effect, one-time map increment, required reason, duplicate override idempotency, and audit metadata. |
| 169 | Replacement proof rule | Domain logic | PASSED | 0 | Implement replacement proof entry on behalf of a team with contributor/admin attribution, proof kind/value validation, and no direct media upload. | 164 | None. | Tests verify URL/text proof handling, invalid URL rejection where relevant, admin attribution, team scoping, and audit metadata. |
| 170 | Override persistence API | Runtime persistence | PASSED | 0 | Add repository methods or Supabase RPC calls for manual reveal, manual hide, quest skip, broken quest override, and replacement proof entry as atomic operations. | 165, 166, 167, 168, 169 | SQL migration files may be excluded through explicit coverage config patterns; reusable adapters must be covered. | Integration tests verify persisted state changes and audit writes for each override path. |
| 171 | Override authorization | Admin route/API | PASSED | 0 | Ensure every override endpoint/server action requires an authenticated admin session and rejects player sessions or unauthenticated requests. | 170 | Route wrappers may be excluded only if they contain no business logic; reusable authorization helpers must be covered. | Tests verify unauthorized users cannot reveal/hide fragments, skip quests, override quests, or enter replacement proof. |
| 172 | Manual reveal admin action | Admin route/API | PASSED | 0 | Add the admin action for manually revealing one or more fragments for a selected team. | 171 | None. | Tests verify valid reveal, max-bound rejection or clamp behavior, team isolation, and audit event creation. |
| 173 | Manual hide admin action | Admin route/API | PASSED | 0 | Add the admin action for manually hiding one or more fragments for a selected team. | 171 | None. | Tests verify valid hide, min-bound rejection or clamp behavior, team isolation, final unlock recalculation, and audit event creation. |
| 174 | Quest skip admin action | Admin route/API | PASSED | 0 | Add the admin action for skipping a selected quest for a selected team with a required reason. | 171 | None. | Tests verify required reason, status update to skipped, no unintended map reveal, and audit event creation. |
| 175 | Broken quest override admin action | Admin route/API | PASSED | 0 | Add the admin action for overriding a broken quest as completed for a selected team with a required reason. | 171 | None. | Tests verify required reason, team quest approval, one map increment, duplicate protection, and audit event creation. |
| 176 | Replacement proof admin action | Admin route/API | PASSED | 0 | Add the admin action for entering replacement proof link/text for a selected team and quest. | 171 | None. | Tests verify proof validation, contributor/admin attribution, pending or approved status behavior as designed, team isolation, and audit event creation. |
| 177 | Override admin UI entry points | Admin UI | PASSED | 0 | Add functional admin controls for manual reveal/hide, quest skip, broken quest override, and replacement proof entry in existing admin screens without introducing dedicated visual polish. | 172, 173, 174, 175, 176 | Static labels may be excluded only through explicit coverage config patterns. | Component/integration tests cover form states, validation messages, successful submissions, and failed authorization/error states. |
| 178 | Override confirmation and reason fields | Admin UI | PASSED | 0 | Require clear admin confirmation or required reason fields for destructive or state-changing overrides, especially manual hide, quest skip, and broken quest override. | 177 | None. | Tests verify reason/confirmation requirements and that accidental empty submissions do not change state. |
| 179 | Pending submissions polling query | Admin data access | PASSED | 0 | Extract pending-submissions fetching into a reusable query or API route suitable for repeated polling and deterministic tests. | 150 | None. | Tests verify pending-only filtering, ordering, team/quest/contributor fields, proof visibility for admin only, and empty state. |
| 180 | Polling client behavior | Admin UI | PASSED | 0 | Add pending-submissions polling every few seconds in the admin view and surface new submissions without manual refresh. | 179 | Browser timer setup may be covered with fake timers; no exception expected for reusable polling logic. | Tests with fake timers verify polling interval, new submission rendering, cleanup on unmount, and no duplicate rows. |
| 181 | Polling error and loading states | Admin UI | PASSED | 0 | Handle polling failures without losing the current pending-submission list, and expose a non-blocking stale/error state for admin. | 180 | None. | Tests verify first-load loading, refresh success, refresh failure with retained data, and recovery on the next successful poll. |
| 182 | Polling interval configuration | Configuration | PASSED | 0 | Define the polling interval in a single constant using a few-second default appropriate for the event. | 180 | Type-only exports may be excluded only through explicit coverage config patterns. | Tests or code review verify polling uses the shared constant rather than hardcoded scattered intervals. |
| 183 | Admin navigation updates | Admin UI | PASSED | 0 | Add admin navigation links to pending submissions, audit log, and override controls while keeping admin-only routing intact. | 162, 177, 180 | Static navigation copy may be excluded only through explicit coverage config patterns. | Component or route tests verify links render only in admin context and target valid routes. |
| 184 | Team privacy regression tests | Security/privacy | PASSED | 0 | Add regression tests proving hints, audit views, overrides, replacement proof, and polling do not expose one team's proof, progress, or private data to the other team. | 159, 163, 176, 180 | Test files themselves may be excluded from coverage. | Tests cover cross-team access attempts and verify no other-team proof/progress is returned. |
| 185 | No media upload regression | Security/privacy | PASSED | 0 | Verify replacement proof and hint/audit/override work continues to store proof as URL/text only and does not add direct media uploads. | 169, 176 | Static scan evidence is excluded from coverage; validation helpers must be covered. | Tests and code scan confirm there are no file inputs, upload endpoints, or media storage paths. |
| 186 | Coverage configuration review | Quality gate | PASSED | 0 | Review coverage exclusions so Phase 5 application logic for hints, audit log, overrides, replacement proof, and polling remains covered at 100%. | 160, 163, 177, 181, 184, 185 | Static assets, SQL migrations, config, type-only files, and tests may be excluded only through explicit narrow patterns. | Coverage config review confirms no Phase 5 business logic is excluded. |
| 187 | Unit and integration coverage gate | Quality gate | PASSED | 0 | Run the Vitest/V8 coverage gate after Phase 5 changes. | 186 | Test files themselves may be excluded from coverage. | `npm run coverage` passes at 100% statements, branches, and functions. |
| 188 | Build and lint verification | Quality gate | PASSED | 0 | Verify Phase 5 changes compile and lint under strict TypeScript and the existing Next.js configuration. | 187 | None. | `npm run build` and `npm run lint` pass from the repository root. |
| 189 | E2E smoke updates | Quality gate | PASSED | 0 | Add or update Playwright smoke coverage for the Phase 5 critical browser behavior: hint use, admin audit log access, override action, replacement proof entry, and pending submission polling. | 188 | E2E specs do not count toward Vitest percentage coverage. | `npm run test:e2e` passes and includes Phase 5 critical flows where practical. |
| 190 | Phase 5 completion review | Quality gate | PASSED | 0 | Review implementation against every Phase 5 acceptance criterion and confirm all tests, coverage, lint, build, and E2E checks have passed. | 187, 188, 189 | Documentation-only review is excluded from coverage. | Every Phase 5 acceptance criterion is mapped to completed tasks and verified by automated tests or explicit review evidence. |
