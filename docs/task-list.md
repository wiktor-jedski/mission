# Task List

Scope: Phase 3 only - Admin Review, Approval, Rejection, And Map Reveal.

Valid Task statuses: OPEN, PREPARED, REJECTED, PASSED
ID: Use growing unique integers

| ID | Component | Static Aspect | Status | Retries | Description | Depends On (ID) | Testing Coverage Exceptions | Verification Criteria |
|----|-----------|---------------|--------|---------|-------------|-----------------|-----------------------------|-----------------------|
| 71 | Phase 3 boundary | Planning and guardrails | PASSED | 0 | Confirm Phase 3 implements only Supabase-backed runtime persistence, admin password login, pending review, approve/reject actions, rejection messages, per-team map progress, full-map unlock, final prize photo access, and the required core-loop Playwright coverage; do not add deployment, QR export, hints, polling, audit-log UI, manual overrides, pause/resume, animations, sound, or visual polish beyond plain accessible UI. | - | Documentation-only review is excluded from coverage. | Implementation note or PR summary states the Phase 3 boundary; deployed runtime behavior uses Supabase as source of truth; code review finds no Phase 4+ or secondary-phase behavior. |
| 108 | Supabase runtime decision | Planning and guardrails | PASSED | 0 | Apply the resolved decision that Phase 3 must replace the Phase 2 local repository runtime path with Supabase-backed reads/writes before any public deployment smoke test. | 71 | Documentation-only review is excluded from coverage. | Phase 3 implementation notes state that local repository runtime state is not acceptable for deployed gameplay; local repository may remain only for tests or explicit fallback. |
| 109 | Supabase server client | Data access | PASSED | 0 | Implement the server-side Supabase client/helper modules needed by runtime route handlers, server actions, and server components. | 108 | Type-only generated database definitions may be excluded through explicit coverage config patterns. | Unit tests cover configured client creation, missing environment handling, and no accidental browser/client leakage of server-only helpers. |
| 110 | Runtime repository interface | Data access | PASSED | 0 | Define a narrow repository interface for Phase 3 runtime operations so player and admin flows can use Supabase without depending on the Phase 2 in-memory implementation. | 109 | Type-only definitions may be excluded through explicit coverage config patterns. | Tests or type checks prove the interface covers team lookup, quest lookup, submissions, progress, pending review, approval, rejection, map state, and audit writes. |
| 111 | Supabase player reads | Data access | PASSED | 0 | Replace runtime player reads for teams, quest access, team submissions, and map state with Supabase-backed queries scoped by the authenticated team. | 110 | None. | Integration tests verify valid team/quest reads, unknown quest safety, team-scoped submissions, team-scoped map state, and no other-team data leakage through Supabase queries. |
| 112 | Supabase player writes | Data access | PASSED | 0 | Replace runtime player proof submission writes with Supabase-backed inserts/updates for submissions and team quest progress. | 111 | None. | Integration tests verify valid submissions persist in Supabase, pending/approved duplicates are blocked, rejected quests can be resubmitted, and writes derive team/quest IDs from trusted server context. |
| 113 | Supabase admin review reads | Data access | PASSED | 0 | Implement Supabase-backed admin queries for pending submissions and optional submission detail views, including team, quest, contributor, note, proof, and progress context. | 110 | None. | Integration tests verify only pending submissions are returned, required review context is included, ordering is stable, and approved/rejected submissions are excluded. |
| 114 | Supabase admin review writes | Data access | PASSED | 0 | Implement Supabase-backed transactional writes for approval, rejection, team quest progress, map progress/final unlock state, and audit events. | 112, 113 | None. | Integration tests verify approval/rejection writes are atomic, idempotent where required, audit events are written, progress is team-scoped, and failed writes leave no partial state. |
| 115 | Local repository runtime removal | Data access | PASSED | 0 | Remove or bypass the Phase 2 local repository from normal runtime routes so deployed gameplay cannot depend on process memory for submissions, review, approvals, or map reveal. | 111, 112, 113, 114 | Test-only local repository code may remain covered by existing tests or excluded only if explicitly test support. | Code review and tests verify runtime routes/server components use the Supabase-backed repository; local repository imports remain only in tests or explicit fallback/test-support code. |
| 116 | Supabase persistence smoke guard | Quality gate | PASSED | 0 | Add a pre-deployment verification step that proves a public deployment smoke test must use configured Supabase persistence rather than local memory. | 115 | Documentation-only checks may be excluded from coverage. | README/docs or test setup state required Supabase env vars for deployed smoke tests; build/test flow fails or clearly errors when deployed runtime persistence is not configured. |
| 72 | Admin session contract | Authentication | PASSED | 0 | Define the server-side admin session shape, cookie name/options, expiration behavior, and helper result types for authenticated, unauthenticated, and invalid-session states. | 71 | Type-only definitions may be excluded through explicit coverage config patterns. | Unit tests cover session creation, parsing, missing cookie, malformed cookie, expired/invalid session, and secure cookie options. |
| 73 | Admin password verification | Authentication | PASSED | 0 | Implement server-side admin password verification against the configured admin password without exposing the expected password or protected admin state on failure. | 72 | None. | Unit/integration tests cover valid password, invalid password, missing password, missing configuration, and no protected-data leakage from failed attempts. |
| 74 | Admin login form UI | Admin UI | PASSED | 0 | Build a plain accessible admin password login form with labels, validation messages, submit state, and no pending-submission data before authentication. | 73 | None. | Component/integration tests verify successful submit, invalid password message, required field handling, and accessible labels. |
| 75 | Admin login handler | Authentication | PASSED | 0 | Wire the admin login form to a server action or route handler that verifies the password, creates the admin session, and redirects only to safe admin paths. | 73, 74 | None. | Tests prove valid password sets only the admin session, invalid password sets no usable session, and redirects preserve safe in-app admin destinations only. |
| 76 | Admin logout path | Authentication | PASSED | 0 | Provide a simple way to clear the admin session for shared-laptop recovery and test isolation. | 72, 75 | None. | Tests verify the admin session cookie is cleared and protected admin routes require login afterward. |
| 77 | Admin auth guard | Authentication | PASSED | 0 | Add a reusable server-side guard for admin routes that requires a valid admin session and redirects unauthenticated users to admin login without loading pending submissions. | 72, 75 | None. | Tests cover unauthenticated access, valid-session access, invalid-session access, and redirect destination handling. |
| 78 | Admin route inventory | Routing | PASSED | 0 | Add or confirm plain admin routes needed for Phase 3: `/admin/login`, `/admin`, and an optional `/admin/submissions/[id]` detail route. | 77 | Static route shell code is covered through route/component tests unless explicitly type-only. | Routes exist, protected routes require admin auth, and unresolved admin submission IDs return a safe not-found state. |
| 79 | Pending submissions query | Data access | PASSED | 0 | Implement data access for pending submissions including team, quest, contributor, note, proof kind/value, submitted time, and current progress context. | 77, 113 | None. | Integration tests verify only pending submissions are returned, rows include required review context, ordering is stable, and approved/rejected submissions are excluded. |
| 80 | Pending submissions review UI | Admin UI | PASSED | 0 | Build the admin review list showing pending submissions with team, quest, contributor, note, proof value, submitted time, and links/actions for review. | 79 | None. | Component/integration tests verify all required fields render for pending submissions and empty-state rendering works. |
| 81 | Proof-link opening behavior | Admin UI | PASSED | 0 | Render URL proof values as admin-only links that open safely from the review view while text proof values remain readable inline. | 80 | None. | Tests verify URL proofs use safe link attributes, text proofs are not treated as external links, and proof values are not rendered outside admin-authenticated views. |
| 82 | Submission detail review view | Admin UI | PASSED | 0 | If needed for ergonomics, build a protected submission detail view for a single pending submission with full team, quest, contributor, note, proof, and action controls. | 79, 81 | None. | Tests cover valid pending submission detail, non-pending submission handling, unknown ID handling, and admin-auth protection. |
| 83 | Review action model | Admin review | PASSED | 0 | Define stable approve/reject action inputs, validation results, rejection reason values, optional rejection message rules, and user-facing error mapping. | 79 | Type-only definitions may be excluded through explicit coverage config patterns. | Unit tests cover every validation branch for approve, reject, missing IDs, invalid IDs, invalid rejection reason, blank/long messages, and trimming behavior. |
| 84 | Submission status transition rules | Domain logic | PASSED | 0 | Implement pure transition rules for pending-to-approved and pending-to-rejected submissions, rejecting invalid transitions such as approving rejected/approved submissions. | 83 | None. | Unit tests cover pending approval, pending rejection, already approved, already rejected, unknown status, and idempotent/no-op expectations. |
| 85 | Map progress calculation | Domain logic | PASSED | 0 | Implement pure map progress helpers that calculate revealed fragment count from approved quest count with a 21-approval cap and final unlock state. | 71 | None. | Unit tests cover 0 approvals, partial approvals, 20 approvals, 21 approvals, more than 21 approvals, and final prize access. |
| 86 | Approval write transaction | Admin review | PASSED | 0 | Implement the approval handler/transaction that marks a pending submission approved, marks the team quest approved, increments that team’s map progress once, and writes an approval audit event. | 84, 85, 114 | None. | Integration tests verify all writes happen atomically, audit event is written, team/quest IDs come from trusted submission data, and failed writes leave no partial state. |
| 87 | Approval idempotency guard | Admin review | PASSED | 0 | Prevent duplicate approvals or repeated action submissions from incrementing map progress more than once for the same team quest. | 86 | None. | Integration tests approve the same quest/submission repeatedly or concurrently where practical and verify the approved quest count/map progress increments exactly once. |
| 88 | Rejection write transaction | Admin review | PASSED | 0 | Implement the rejection handler/transaction that stores reason/message, marks the submission rejected, marks team quest progress rejected, allows resubmission, and writes a rejection audit event. | 83, 84, 114 | None. | Integration tests verify rejection fields persist, progress becomes rejected, a new player submission is allowed afterward, audit event is written, and failed writes leave no partial state. |
| 89 | Admin review action UI | Admin UI | PASSED | 0 | Add approve and reject controls to the admin review view, including rejection reason/message fields, submission state feedback, and accessible error display. | 86, 88 | None. | Component/integration tests cover successful approval, successful rejection, validation errors, disabled/pending submit states, and disappearing or updated pending rows after action. |
| 90 | Admin safe not-found states | Security | PASSED | 0 | Ensure unknown, unauthorized, or non-pending admin submission review states do not leak unnecessary proof data or internal IDs. | 77, 82, 89 | None. | Tests verify unknown IDs, malformed IDs, and non-pending submissions render safe states behind admin auth and reveal no unrelated team/proof data. |
| 91 | Player map route | Routing | PASSED | 0 | Add or confirm the logged-in player map route, such as `/map`, scoped to the current team session. | 85 | Static route shell code is covered through route/component tests unless explicitly type-only. | Route exists, unauthenticated users are redirected to login, and authenticated users only load their own map state. |
| 92 | Team map state query | Data access | PASSED | 0 | Implement team-scoped data access for the current team’s approved quest count, revealed fragment count, full-map state, and final prize access. | 91, 111 | None. | Integration tests seed both teams and verify each team receives only its own progress and unlock state. |
| 93 | Map reveal UI | Player UI | PASSED | 0 | Build the player map view showing exactly the logged-in team’s revealed fragment count and full-map state when unlocked, using plain accessible UI. | 92 | None. | Component/integration tests cover 0 fragments, partial fragments, 20 fragments, 21 fragments, and no other-team progress visibility. |
| 94 | Final prize photo access | Player UI | PASSED | 0 | Gate final prize photo access behind the team’s 21 approved quests/full-map state and keep it unavailable before unlock. | 92, 93 | Static image assets may be excluded through explicit coverage config patterns. | Tests verify the final photo/link is hidden before 21 approvals, visible at 21 approvals, remains visible afterward, and is scoped per team. |
| 95 | Player progress refresh after approval | Player flow | PASSED | 0 | Ensure a team sees updated map progress after an admin approval by reloading or navigating to the map page without relying on client-side polling. | 86, 92, 93 | None. | Integration or Playwright tests verify map progress changes from pending/partial to the next revealed count after approval. |
| 96 | Cross-team privacy enforcement | Security | PASSED | 0 | Ensure admin approval/rejection for one team never changes the other team’s quest status, map progress, revealed fragment count, or final prize access. | 86, 88, 92, 94 | None. | Integration tests seed both teams, process actions for one team, and assert the other team’s state remains unchanged and private. |
| 97 | Admin-only proof visibility | Security | PASSED | 0 | Verify proof values, notes, contributor details, and review actions are visible only to authenticated admin review surfaces and the submitting team where Phase 2 allows status display. | 80, 81, 89, 93 | None. | Tests prove unauthenticated users, the other team, and generic player map views cannot see admin-only proof/review data. |
| 98 | Rejection resubmission integration | Player flow | PASSED | 0 | Verify the Phase 2 rejected-resubmission player flow works with real Phase 3 rejection actions rather than only seeded rejected data. | 88 | None. | Integration tests cover submit proof, admin rejects with message, team sees rejection, team resubmits immediately, and new submission is pending. |
| 99 | Final unlock integration | Player flow | PASSED | 0 | Verify 21 distinct approved quests unlock full map state and final prize access for that team. | 86, 87, 94, 96 | None. | Integration tests approve 21 distinct quests for one team and assert full map/final photo access for that team only; 20 approvals remains locked. |
| 100 | Admin review tests | Test coverage | PASSED | 0 | Add unit/integration tests for admin login, protected admin routes, Supabase-backed pending submission review, proof link rendering, approve action, reject action, audit writes, and duplicate approval prevention. | 72, 73, 77, 80, 86, 87, 88, 89, 113, 114 | Test files themselves may be excluded from coverage. | `npm test` includes failing and passing cases for every Phase 3 admin review acceptance criterion. |
| 101 | Map reveal tests | Test coverage | PASSED | 0 | Add unit/integration tests for Supabase-backed per-team map progress, revealed count calculation, full-map unlock, final prize access, and cross-team privacy. | 85, 92, 93, 94, 96, 99, 111, 114 | Test files themselves may be excluded from coverage. | `npm test` includes map reveal and final unlock scenarios at 0, partial, 20, 21, and cross-team states. |
| 102 | Core-loop Playwright smoke | E2E | PASSED | 0 | Add or update Playwright smoke coverage for the Phase 3 core loop using persisted Supabase-backed state: team login -> open quest -> submit proof -> admin login -> approve submission -> team sees map reveal. | 75, 80, 86, 91, 93, 95, 115 | E2E specs do not count toward Vitest percentage coverage. | `npm run test:e2e` passes for the required core loop on a local dev server and does not rely on process-memory repository state. |
| 103 | Coverage configuration review | Quality gate | PASSED | 0 | Review coverage exclusions so only static assets, SQL migrations, type-only files, and explicit config artifacts are excluded; all Phase 3 application logic, including Supabase repository adapters, must be covered. | 100, 101, 115 | Static/type-only/config exclusions must be explicit and narrow. | Coverage report shows 100% statements, branches, and functions for committed Phase 3 application code. |
| 104 | Coverage gate verification | Quality gate | PASSED | 0 | Run the Vitest/V8 coverage gate after Phase 3 implementation. | 100, 101, 103 | Test files themselves may be excluded from coverage. | `npm run coverage` passes at 100% statements, branches, and functions. |
| 105 | Build and lint verification | Quality gate | PASSED | 0 | Verify Phase 3 changes compile and lint under strict TypeScript and the existing Next.js configuration. | 104 | None. | `npm run build` and `npm run lint` pass from the repository root. |
| 106 | Phase 3 README/env review | Documentation | PASSED | 0 | Update documentation for Phase 3 Supabase runtime requirements and any new environment variables, commands, routes, final-photo asset expectations, or local test setup details. | 75, 94, 102, 116 | Documentation-only changes are excluded from coverage. | README/docs remain accurate; Supabase runtime persistence, admin password, final prize photo setup, and any new routes are documented where needed. |
| 107 | Phase 3 completion review | Quality gate | PASSED | 0 | Review implementation against every Phase 3 acceptance criterion and confirm no deployment, QR generation, hints, polling, overrides, pause/resume, animation, sound, or visual polish scope leaked into this phase. | 71, 97, 98, 99, 102, 104, 105, 106, 116 | Documentation-only review is excluded from coverage. | Review confirms Supabase-backed runtime persistence, admin password protection, pending review, proof opening, approval, rejection, idempotent map progress, team-private map reveal, 21-approval final unlock, Playwright core loop, and quality gates are complete. |
